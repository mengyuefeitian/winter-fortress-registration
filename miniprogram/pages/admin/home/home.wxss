/* pages/admin/home/home.wxss - Minimal Style, rely on global styles */

/* 分区选择器样式 */
.form-row {
  display: flex;
  align-items: center;
  gap: 16px;
}

.form-col {
  flex: 1;
}

.form-label {
  font-size: 13px;
  color: #5F5F5F;
  margin-bottom: 6px;
  display: block;
}